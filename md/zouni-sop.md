# 走你 · 项目 SOP v0.5

> **这份 SOP 自己也要迭代。** 每次踩坑、每次复盘都回来改它，版本号往上走，变更记录在末尾。

## 目录与分工

| 文档 | 管什么 | 谁维护 |
|---|---|---|
| **本文档** | 怎么干活：三条工作线 + 进化机制 | 一起 |
| `zouni-engine-prd-v1.md` | 引擎架构、数据结构定义 | 我 |
| `zouni-release-checklist.md` | 九连测、人工点检、红线、发布操作 | 我 |
| `zouni-content-audit.md` | 21 条线路覆盖度矩阵、内容 backlog | 我，你定优先级 |

---

## 零、总纲：三条线并行 + 一个进化机制

```
功能线  ── 每周   ── 我提案 → 你审核 → 进 backlog → 开发
内容线  ── 每周  ── 补线路/变体 → 数据体检 → 更新矩阵
工程线  ── 每次改动 ── 八步审查（含发布）── 测试全绿即上线，不攒批次
              ↓
        进化机制：事故 → 根因 → 红线 → 自动断言 → 回写 SOP
```

三条线的共同规矩：**产出物是证据，不是承诺**。每条线每轮都要留下可复查的东西（提案单 / 矩阵更新 / 测试报告）。

---

## 一、角色与决策边界

| | 你（产品） | 我（设计 + 研发 + 测试 + 内容） |
|---|---|---|
| **拍板** | 做什么、给谁用、何时发、内容取舍、视觉方向认可 | 怎么实现、什么结构、测试怎么写 |
| **提出** | 现象、优先级、真机反馈 | 根因、方案选项、工作量与风险、功能提案 |
| **不做** | 不必看代码，不必指定技术方案 | 不替你定产品，不擅自扩大改动范围 |

**冲突时**：产品定义你说了算；工程红线我说了算（那些都有事故为证，破一条就复发）。

---

## 二、功能线：定期产品审查

### 节奏
**每周一次**。我准备《功能提案单》，你逐条批。

### 提案来源（我主动扫描，不等你提）
| 来源 | 怎么扫 |
|---|---|
| 用户旅程断点 | 走一遍"挑→排→改→存→分享→出行"，找哪一步会卡住或需要离开产品 |
| 体检报告 ℹ 项 | 第 8 项报的覆盖度缺口、第 9 项的可访问性边界 |
| 真机反馈 | 你发的截图和吐槽，归类后看有无共性 |
| 外部对照 | 定期读一份优秀设计系统/产品的公开规范，逐条对照我们（已做过一次 Vercel，抓出对比度/表格语义/焦点环三项） |
| 数据能力 | 引擎已具备但界面没用上的能力（如插入机制、多币种、hash 传递） |

### 提案单格式（每条必须写全，缺项不递）
```
【问题】现在用户遇到什么 / 差什么
【证据】截图、体检报告某项、或具体场景复现路径
【方案】做什么，做到什么程度
【工作量】小(半天内) / 中(1-2 天) / 大(需拆期)
【风险】可能影响什么、要动哪些红线
【不做的理由】如果我建议不做，理由写这里
```

### 你的审核动作
每条只需回三个字之一：**做 / 缓 / 否**，加一句优先级或补充。通过的进 backlog，我排期。

### 已知候选（下次审查可先看这些）
- 行程内"今天实际花了多少"记账（引擎有费用结构，界面没入口）
- 多人协作：同一份行程两人各自勾清单（需后端，先评估必要性）
- 离线可用性增强（SW 已有，可做"出发前一键缓存全程"）
- 行程导出为日历（.ics）或图片长图
- 首页按"离你多远/什么季节"排序（现在是固定分组）

---

## 三、内容线：线路卡片持续扩充

### 现状矩阵（15 族 21 条）
| 维度 | 现有 | 缺口 |
|---|---|---|
| **国内自驾** | 12 族（北疆、青甘、川西、稻城、西藏、滇西北、梅里、额济纳、呼伦贝尔、桂林、张家界、黄山） | 已较厚，主要缺**天数变体** |
| **国内公共交通** | 2 族（香港地铁渡轮、都江堰动车） | ⚠ 太薄。高铁/城际类线路几乎空白 |
| **海外** | 1 族（日本关西，电车） | ⚠ 最大缺口 |
| **步行/徒步为主** | 0 族独立（雨崩、黄山是自驾+徒步组合） | 城市漫步类完全空白 |
| **天数变体** | 4 族有多天（xj/qg/cx/sc），11 族单方案 | ⚠ 用户改不了天数 |
| **省份覆盖** | 11 / 34 个省级行政区 | ⚠ 缺 23 个（含全部直辖市与东部大省） |

### 内容路线图（你定的优先级）

**批次一：天数变体**（当前最高优先级）
11 个单方案族每族补到至少 2 个变体。用户最常问"我只有 X 天"，这是转化断点。

**批次二：国内省份覆盖** — 目标：每个省级行政区 + 重点城市**至少 1 条**
| 状态 | 省级行政区 |
|---|---|
| ✅ 已有（11） | 新疆 青海 甘肃 四川 云南 西藏 内蒙古 安徽 广西 湖南 香港 |
| ⬜ 待补（23） | 北京 上海 天津 重庆 · 黑龙江 吉林 辽宁 · 河北 山西 · 江苏 浙江 山东 江西 福建 · 河南 湖北 · 广东 海南 · 贵州 陕西 宁夏 · 澳门 台湾 |

**补的顺序：按旅行热度排，不按行政区划排**（你定的）。

| 梯队 | 目的地 | 缺的省份 | 主要形态 |
|---|---|---|---|
| **T1 最热** | 北京 · 上海 · 西安 · 重庆 · 杭州 · 厦门 · 三亚 · 哈尔滨（冬） | 京 沪 陕 渝 浙 闽 琼 黑 | 公共交通 / 城市漫步为主 |
| **T2 次热** | 苏州南京 · 广州深圳 · 青岛 · 武汉 · 长沙 · 贵州（西江千户苗寨） · 大理丽江补线 | 苏 粤 鲁 鄂 湘补 黔 | 高铁 + 短途自驾 |
| **T3 补全** | 洛阳开封 · 平遥 · 秦皇岛承德 · 南昌庐山 · 长春吉林 · 沈阳 · 银川 · 澳门 台湾 | 豫 晋 冀 赣 吉 辽 宁 澳 台 | 混合 |

T1 优先的理由：既是最大出发地也是最热目的地，且以公共交通为主 —— 正好补上我们当前"公共交通仅 2 条"的最大形态缺口，制作成本也低于长线自驾。

**批次三：海外**（你定的顺序）
```
港澳台 → 东南亚 → 日韩 → 欧美 → 南极北极 → 其他海外
```
| 批 | 地区 | 说明 |
|---|---|---|
| 1 | 港澳台 | 香港已有；补澳门、台北/环岛 |
| 2 | 东南亚 | 泰越新马、巴厘、菲律宾 — 签证简单、成本低、需求大 |
| 3 | 日韩 | 关西已有；补东京、北海道、首尔/济州 |
| 4 | 欧美 | 西欧多国、美西、美东 — 需处理多国跨境与多币种 |
| 5 | 南极北极 | 极地邮轮/破冰 — 高客单、低频，形态特殊（需新的行程结构） |
| 6 | 其他海外 | 中东、非洲、南美、大洋洲 |

**交通形态覆盖**（横切三个批次）：自驾 / 公共交通（高铁地铁）/ 步行城市漫步 / 混合。
当前 21 条里公共交通只有 2 条、纯步行 0 条 — 每批新线路里至少一半走非自驾形态。

### 每条新线路的生产标准（六项，缺一不可）
1. **Schema v2 三件套**：`transport` / `locale` / `stay`（旧字段由 `normRoute` 反写，不手写）
2. **逐日行程**：站点、时窗、接驳、住宿链（`lodges` 项数 = 天数，末天 `null`）
3. **地图**：`nodes` / `order` / `seg`（长度 = 天数）/ `tonight`（长度 = 天数）；往返段自动弧线分离
4. **价格两处同步**：路线 `budgets` + 首页卡片文案
5. **i18n**：中英双写；路线级建议挂 `i18n:{en:{...}}` 覆盖层
6. **至少 1 个可插入备选点**：插到哪天、接哪个站后、时长、价格、前后接驳、一句话推荐

写完跑第 2、8 项验证，更新内容矩阵。

### 节奏：先冲刺，后维持

| 阶段 | 目标 | 节奏 |
|---|---|---|
| **冲刺期**（当前） | 线路总数 **21 → 100 条** | 按批次密集生产，每轮尽可能多产出 |
| **维持期**（100 条之后） | 保鲜与查漏 | 每周 1–2 条新增或更新 |

**100 条怎么拆**（21 条已有，需新增 79 条）：
| 批 | 内容 | 条数 | 累计 |
|---|---|---|---|
| 1 | 11 个单方案族各补 1–2 个天数变体 | ~16 | 37 |
| 2 | 国内热门目的地（见下方顺序）新族 + 变体 | ~35 | 72 |
| 3 | 海外六批（港澳台→东南亚→日韩→欧美→极地→其他） | ~28 | 100 |

每月回顾一次矩阵，砍掉不做的，避免为凑数而凑数。

---

## 四、工程线：八步审查（不可跳）

```
① 侦察 → ② 划界 → ③ 预检 → ④ 落盘 → ⑤ 回归 → ⑥ 眼验 → ⑦ 同步 → ⑧ 发布
```

**⑧ 是默认动作，不是可选项**：每轮改动做完、测试全绿，就直接更新到线上，不攒批次、不等你催。没发布 = 这轮没做完。

| 步 | 动作 | 审查产出物（证据） |
|---|---|---|
| **① 侦察** | 读出待改处的**真实文本**，不凭记忆 | 贴出实际代码片段 |
| **② 划界** | **确定最小改动范围**：列出要动的文件/函数/规则，以及"确认不动"的边界 | 影响面声明（见下表） |
| **③ 预检** | 两阶段补丁：所有替换先 `count==1` 全量校验，**全过才写盘** | 预检通过/失败的明确输出 |
| **④ 落盘** | 改在原位；删 CSS 必须整条规则删 | 落盘确认 + 语法检查通过 |
| **⑤ 回归** | 按影响面跑对应回归项 + 九连测；**新功能必须同时新增断言** | 回归清单勾选 + 九连测输出 |
| **⑥ 眼验** | 改了界面必须 headless Chrome 真渲染截图，我先看 | 截图 |
| **⑦ 同步** | 五产物 + 站点包 + 文档同步，版本号递进 | 产物清单 |
| **⑧ 发布** | 九连测全绿 → 推送上线 → **线上真浏览器复验** → 报告"改了什么/根因/怎么验的/影响面多大/什么没做" | LIVE_URL 复验结果 + 说明 |

**"改好了"三个字，必须以截图为凭。** jsdom 没有布局引擎，测不出排版。

### 最小改动范围三条纪律
1. **只动必须动的**：你说改 A，就只改 A。发现 B 也该改 → 先说，你定，另开一轮。
2. **改动要可局部化**：优先改单个函数/单条 CSS 规则，避免改公共层（token、统一层、委托链）；必须改公共层时，在报告里明确标注"这次动了公共层"。
3. **改完能说清没影响谁**：说不清影响面 = 改动范围没划清楚，回到 ②。

### 影响面 → 必跑回归项
| 你动了什么 | 必跑 | 为什么 |
|---|---|---|
| **单条线路数据** | 第 2 引擎仿真 + 第 8 数据体检 + 卡片价格对账 | 时刻/预算/住宿链/地图易错位 |
| **引擎逻辑**（resolve/totals/normRoute） | 第 2 全量仿真 + 第 4 冒烟 + 抽 3 条线人工核对费用 | 一处改动波及 21 条线 |
| **CSS**（任何） | 第 6 视觉回归 + 第 9 可访问性 + **三页截图肉眼过** | 孤儿规则、裁切、对比度、焦点环都在这里翻车过 |
| **公共层**（token / 统一层 / 组件基类） | 第 6 + 第 9 + 全 Tab 截图 + E2E | 影响面最大，必须全量视觉复验 |
| **交互委托**（click 链、handler） | 第 7 E2E 真点击 + 第 4 冒烟 | 新分支被前面分支截胡是已知坑 |
| **i18n 字典 / 文案** | 第 3 静态终检（双写对账）+ 中英切换冒烟 | 单边写入、键缺失、函数名遮蔽 |
| **持久化结构** | LS 往返 + 旧版本兼容 + 导入导出 + 清数据 | 用户数据不可逆 |
| **发布装配**（PWA/SW/站点包） | 第 5 三文件冒烟 + 线上复验 | file:// 与 https 行为不同 |

### 关键路径（上线前人工必过，挂了等于产品废了）
> 详细版见 `zouni-release-checklist.md` 第二节。这十条是**最小必过集**：

1. 微信打开首页 → 1 秒内出内容，无报错横幅
2. 点任一线路卡 → 行程能出来
3. 切天数 / 改预算 → 结果实时重排，金额跟着变
4. 切某一天 → 时间轴内容正确切换
5. 搜索输入中文 → 有结果，点进去能安排
6. 存进行程 → 杀掉微信重进 → 行程还在
7. 分享 → 粘贴板里有可读的完整摘要
8. 中/EN 切换 → 全站文案跟随，不留半中半英
9. 货币切换 → 所有金额换算，刷新后记住
10. 「我的」→ 清除数据两步确认 → 回到初始态

---

## 五、质量门禁

**自动九连测**（`bash zouni-tests/run-all.sh`）：语法 → 引擎仿真 → 静态终检 → 双冒烟 → 视觉回归 → E2E → 数据体检 → 可访问性。明细见发布清单第一节。

**人工真机**（只有你能做）：约 20 项，**必须在微信里点**。headless Chrome ≠ 微信 X5 ≠ iOS Safari。

### 缺陷分级
| 级别 | 定义 | 响应 |
|---|---|---|
| **P0** | 报错、白屏、算错、点了没反应 | 停下手上的事先修 |
| **P1** | 排版错乱、假按钮、文案骗人、可访问性不达标 | 当轮修 |
| **P2** | 视觉细节、间距、文案优化 | 攒批次 |
| **Backlog** | 内容缺口 | 进内容矩阵排期 |

---

## 六、发布流程

**常态：每轮改动完成即发布**，不攒批次。唯一前提是**过审**。

```
九连测全绿 → 关键路径回归 → 眼验截图 → 发布 → 线上真浏览器复验
        ↑ 任一不过 = 不发布，先修
```

| 门槛 | 要求 | 谁来验 |
|---|---|---|
| 九连测 | 全绿，一项不过都不发 | 我（自动） |
| 关键路径 | 十条最小必过集（发布清单一·五节） | 我（自动）+ 你（真机） |
| 眼验 | 改了界面必须有截图，我先看过 | 我 |
| 线上复验 | 发布后真浏览器打开 LIVE_URL，验渲染与零报错 | 我 |
| 真机点检 | 微信安卓 / 微信 iOS / iOS Safari | **只有你能做** |

**我发布后线上就是可用的**；你的真机点检是加保险，不是发布前置条件（内测期）。公开推广前，真机点检必须补齐。

**发布频率有平台限制**：GitHub Pages 同一小时内推送十余次后会开始拒绝构建（表现为 `duration: 0` 的失败，与代码无关）。所以"每轮做完即发"要理解为**每轮攒齐再发一次**，而不是每改一处就推一次。详见发布清单「发布频率纪律」。

1. 发布：`GITHUB_PUBLISH_TOKEN='ghp_xxx' python3 /tmp/quickship_publish.py --dir zouni-site --name zouni`
2. 线上复验：微信打开 LIVE_URL 过人工清单，iOS 验"添加到主屏"
3. 回滚：重跑同名发布即覆盖；每轮产物都是完整快照

微信内 github.io 不稳时：`zouni-site/` 原样传国内静态托管或自有域名，代码零改动。

---

## 七、进化机制（这份 SOP 怎么变聪明）

> **总目标：用最小的代价和成本，提升效率与质量。**
> 每加一条流程/测试/规则前先问：它能防住什么真实事故？代价多大？
> 防不住具体事故的流程 = 仪式，删掉。跑一次超过 3 分钟的测试 = 没人跑，要么提速要么拆分。

### 1. 事故闭环（四步，缺一条就会复发）
```
现象 → 根因 → 修复 → 【红线 + 自动断言】
```
只修不写红线 = 下次还犯；只写红线不加断言 = 靠记性，迟早忘。
**已闭环的例子**：
| 事故 | 红线 | 自动断言 |
|---|---|---|
| 微信白屏 | 禁 `?.` 等新语法 | 静态终检第 ⑤ 项 |
| 排版全乱 | jsdom 测不了排版 | 视觉回归第 6 项 |
| 左列按钮没边框 | 裁切容器内用内描边 | 第 9 项 + 截图 |
| 文案承诺"可加"却加不了 | 承诺必须有数据 | 静态终检第 ⑧ 项 |
| 焦点环被覆盖 | 末尾高特异性兜底 | 第 9 项真实 Tab |

### 2. 复盘节奏
| 时机 | 做什么 | 时长 |
|---|---|---|
| 每次发布后 | 这轮踩了什么坑？闭环了吗？ | 5 分钟 |
| 每周（功能审查时） | 上批做的有用吗？哪些该做没做？ | 15 分钟 |
| 每月 | 三条线的节奏对不对？SOP 哪条没人执行？ | 30 分钟 |

**复盘只问三个问题**：① 什么起作用了 ② 什么白做了 ③ 什么该做没做。

### 3. 外部学习
每月至少读一份外部优秀规范（设计系统、产品文档、工程实践），**逐条对照我们实测**，不是照抄。
已做：Vercel 设计规范 → 抓出对比度不达标、表格无语义、焦点环被覆盖三项真缺陷。
纪律：**brief 优先**——碱白规范是我们的既定 brief，外部规范只提供判据，不改我们的视觉语言。

### 4. SOP 自迭代
- 每次新增红线 / 新增测试项 / 改节奏 → 回来更新本文档，版本号 +0.1
- 末尾维护变更记录，写清楚**因为什么事故或什么发现而改**
- 每月大复盘时检查：哪条 SOP 从没被执行过？要么删掉，要么找出没执行的原因
- **只增不减 = 官僚化**。每次加新规则时，看看有没有旧规则可以合并或删除；SOP 篇幅长期失控就是失效的信号

---

## 八、沟通约定

- **提问题给现象就够**：截图 + 一句话，根因是我的活
- **我不说"应该没问题"**：要么给验证过程，要么明说没验证
- **不做假东西**：假按钮、占位符、"即将上线"、承诺可加却加不了 — 一律不交付
- **不擅自扩大范围**：你说改 A 我不顺手改 B；B 确实该改我先说，你定
- 中文交流，移动端优先，微信是第一交付渠道

---

## 九、已定事项（2026-08-06 拍板）

| # | 事项 | 结论 |
|---|---|---|
| 1 | 功能审查节奏 | **每周一次** |
| 2 | 内容补充节奏 | **冲刺到 100 条**，之后每周 1–2 条 |
| 3 | 内容优先级 | 天数变体 → 国内省份全覆盖 → 海外分批 |
| 4 | 国内补充顺序 | **按旅行热度排**（T1 北上西渝杭厦琼哈 → T2 → T3），不按行政区划 |
| 5 | 发布节奏 | **每轮做完即发**（过审为前提）；公开后固定周节奏 |
| 6 | 红线增补权 | **我直接加**（都有事故为证），加完在报告里告知 |
| 7 | 真机矩阵最小集 | 微信安卓 + 微信 iOS + iOS Safari |
| 8 | SOP 是否进发布包 | **进**，作为交接材料 |

## 变更记录

| 版本 | 日期 | 改了什么 | 触发原因 |
|---|---|---|---|
| v0.1 | 2026-08-06 | 初稿：角色、六步、质量门禁、发布流程 | 项目积累到需要固化 |
| v0.2 | 2026-08-06 | 增加功能线（定期产品审查）、内容线（线路扩充矩阵与生产标准）、进化机制（事故闭环 / 复盘节奏 / 外部学习 / SOP 自迭代） | 你指出 v0.1 只覆盖开发环节 |
| v0.5 | 2026-08-06 | 七步→八步，发布并入标准流程成为默认收尾；发布流程节明确五道门槛与责任人 | 你确认「每次做完就更新到线上，通过测试审查才更新」 |
| v0.4 | 2026-08-06 | 八条待定项全部拍板落地：功能审查改每周、内容冲刺 100 条路线拆解、国内按热门梯队 T1-T3 排序、SOP 随包交付 | 你的拍板 |
| v0.3 | 2026-08-06 | 六步→七步（新增「划界」）、最小改动范围三条纪律、影响面→回归项映射表、关键路径十条必过集、内容路线图（省份 11/34 + 海外六批）、进化机制加「最小代价」总目标与 SOP 精简条款 | 你要求：最小改动范围、详细回归清单防上线不 run、内容优先级明确、SOP 以最小代价提效提质 |
